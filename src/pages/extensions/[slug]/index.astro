---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Icon from '../../../components/Icon.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const extensions = await getCollection('extensions');
  return extensions.map((ext) => ({
    params: { slug: ext.slug },
    props: { ext },
  }));
}

const { ext } = Astro.props;
const { slug } = Astro.params;
const { Content } = await ext.render();

// Setup SoftwareApplication schema for Google search rich snippet
const structuredData = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": ext.data.title,
  "operatingSystem": "Chrome, Firefox, Edge, Safari",
  "applicationCategory": "BrowserApplication",
  "aggregateRating": {
    "@type": "AggregateRating",
    "ratingValue": "4.8",
    "ratingCount": "84"
  },
  "offers": {
    "@type": "Offer",
    "price": "0.00",
    "priceCurrency": "USD"
  }
};
---

<BaseLayout
  title={ext.data.title}
  description={ext.data.tagline}
  structuredData={structuredData}
>
  <!-- Hero Section -->
  <section class="py-32 bg-bg border-b border-border">
    <div class="max-w-6xl mx-auto px-6 grid grid-cols-1 lg:grid-cols-12 gap-16 items-center">
      <!-- Left: Extension info -->
      <div class="lg:col-span-6 flex flex-col items-start gap-6 text-left">
        <!-- Back Link -->
        <a href="/extensions" class="inline-flex items-center gap-1.5 text-mono text-xs text-muted hover:text-text transition-colors mb-2 uppercase tracking-wider">
          <Icon name="ArrowLeft" class="w-3.5 h-3.5" />
          Back to Directory
        </a>
        
        <div class="flex items-center gap-4">
          <div class="w-16 h-16 rounded-2xl bg-accent flex items-center justify-center text-white shadow-sm">
            <Icon name={ext.data.icon} class="w-9 h-9 text-white" />
          </div>
          <div>
            <h1 class="text-h1 text-text leading-tight">
              {ext.data.title}
            </h1>
            <p class="text-mono text-xs text-muted mt-1">
              Version {ext.data.version} &bull; Updated {ext.data.lastUpdated}
            </p>
          </div>
        </div>

        <p class="text-body text-muted max-w-prose">
          {ext.data.tagline}
        </p>

        <!-- CTAs -->
        <div class="flex flex-col gap-2.5 w-full sm:w-auto mt-2">
          <div class="flex flex-col sm:flex-row items-stretch sm:items-center gap-3 w-full sm:w-auto">
            <a
              href={ext.data.storeUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="bg-accent hover:bg-accent-hover text-white px-8 py-3.5 rounded-lg font-semibold transition-colors duration-150 shadow-sm text-center flex items-center justify-center gap-2 group focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent outline-none cursor-pointer"
            >
              Add to Chrome (Free)
              <Icon name="ArrowUpRight" class="w-4 h-4 transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5" />
            </a>
            <a
              href={`/extensions/${slug}/privacy`}
              class="border border-border bg-surface text-text hover:border-text px-6 py-3.5 rounded-lg font-semibold transition-all duration-150 text-center focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent outline-none cursor-pointer"
            >
              Privacy Policy
            </a>
          </div>
          <p class="text-[11px] text-muted text-left mt-1">
            Free to install &bull; Pro upgrade available for advanced features
          </p>
        </div>

        <!-- Meta badges -->
        <div class="flex flex-wrap items-center gap-6 mt-4 text-mono text-xs text-muted">
          <div class="flex items-center gap-1.5">
            <Icon name="Download" class="w-4 h-4 text-accent" />
            <span>{ext.data.downloadCount}</span>
          </div>
          <span>&bull;</span>
          <div class="flex items-center gap-1.5">
            <Icon name="Shield" class="w-4 h-4 text-green-600" />
            <span>GDPR Compliant</span>
          </div>
          <span>&bull;</span>
          <div class="flex items-center gap-1.5">
            <Icon name="Github" class="w-4 h-4 text-text" />
            <span>Open Source</span>
          </div>
        </div>
      </div>

      <!-- Right: Real Toolbar/Popup UI rendered in CSS -->
      <div class="lg:col-span-6 flex justify-center w-full">
        <div class="w-full max-w-2xl lg:max-w-none bg-surface rounded-xl border border-border shadow-md overflow-hidden flex flex-col transition-all duration-200">
          <!-- Window Header -->
          <div class="bg-surface-2 px-4 py-3 border-b border-border flex items-center justify-between">
            <div class="flex gap-1.5">
              <span class="w-2.5 h-2.5 rounded-full bg-[#FF5F57]"></span>
              <span class="w-2.5 h-2.5 rounded-full bg-[#FFBD2E]"></span>
              <span class="w-2.5 h-2.5 rounded-full bg-[#27C93F]"></span>
            </div>
            <!-- Mock URL bar -->
            <div class="bg-surface border border-border rounded-md px-3 py-0.5 text-mono text-xs text-muted w-40 text-center truncate select-none">
              {slug === 'tab-snoozer' ? 'github.com/BRAMrepos/bramlabs' : 'instagram.com'}
            </div>
            <!-- Mock extension icon in toolbar -->
            <div class="w-10 flex justify-end">
              <span class="w-5 h-5 rounded bg-accent flex items-center justify-center text-xs text-white">
                <Icon name={ext.data.icon} class="w-3.5 h-3.5 text-white" />
              </span>
            </div>
          </div>

          <!-- Popup / Browser Split Content -->
          <div class={slug === 'tab-snoozer' ? "p-6 bg-surface min-h-[320px] flex flex-col justify-between text-left" : "bg-surface min-h-[380px] flex flex-col justify-between text-left"}>
            <!-- Render custom popup based on the extension -->
            {slug === 'tab-snoozer' ? (
              <div class="flex flex-col gap-5 flex-grow">
                <!-- Popup Header -->
                <div class="flex items-center gap-3 border-b border-border pb-3">
                  <div class="w-8 h-8 rounded-lg bg-accent flex items-center justify-center text-white text-sm font-bold">TS</div>
                  <div>
                    <h4 class="font-heading font-semibold text-text text-sm">Tab Snoozer</h4>
                    <p class="text-xs text-muted">Snooze active tab</p>
                  </div>
                </div>
                <!-- Snooze presets -->
                <div class="flex flex-col gap-2">
                  <span class="text-mono text-[10px] font-bold text-muted uppercase tracking-wider">Presets</span>
                  <div class="grid grid-cols-1 gap-2">
                    <button class="flex items-center justify-between border border-border hover:border-accent hover:bg-accent/5 rounded-lg p-2.5 text-xs text-left transition-colors font-medium">
                      <span class="flex items-center gap-2"><Icon name="Clock" class="w-4 h-4 text-accent" /> Later Today</span>
                      <span class="text-muted font-mono text-[10px]">In 3 hours</span>
                    </button>
                    <button class="flex items-center justify-between border border-border hover:border-accent hover:bg-accent/5 rounded-lg p-2.5 text-xs text-left transition-colors font-medium">
                      <span class="flex items-center gap-2"><Icon name="Calendar" class="w-4 h-4 text-accent" /> Tomorrow Morning</span>
                      <span class="text-muted font-mono text-[10px]">09:00 AM</span>
                    </button>
                    <button class="flex items-center justify-between border border-border hover:border-accent hover:bg-accent/5 rounded-lg p-2.5 text-xs text-left transition-colors font-medium">
                      <span class="flex items-center gap-2"><Icon name="Coffee" class="w-4 h-4 text-accent" /> Next Weekend</span>
                      <span class="text-muted font-mono text-[10px]">Saturday</span>
                    </button>
                  </div>
                </div>
                <!-- Mini warning / note -->
                <div class="text-xs text-muted bg-surface-2 border border-border rounded p-2.5 flex items-start gap-2">
                  <Icon name="Info" class="w-3.5 h-3.5 text-accent flex-shrink-0 mt-0.5" />
                  <span>Your active tab will be securely closed and reopened automatically.</span>
                </div>
              </div>
            ) : (
              <div class="flex flex-row flex-grow bg-white dark:bg-black text-text select-none min-h-[380px] overflow-hidden">
                <!-- Left Instagram Mockup (62% width, hidden on extra small mobile screens) -->
                <div class="hidden sm:flex flex-row w-[62%] border-r border-[#efefef] dark:border-[#262626] bg-white dark:bg-[#000000] text-black dark:text-[#f5f5f5]">
                  <!-- Instagram Mini Vertical Navbar -->
                  <div class="w-12 border-r border-[#efefef] dark:border-[#262626] py-4 flex flex-col items-center gap-5 text-[#262626] dark:text-[#a8a8a8] bg-white dark:bg-black">
                    <Icon name="Instagram" class="w-4.5 h-4.5 text-black dark:text-white" />
                    <Icon name="Home" class="w-4.5 h-4.5 text-black dark:text-white" />
                    <Icon name="Search" class="w-4.5 h-4.5" />
                    <Icon name="Compass" class="w-4.5 h-4.5" />
                    <Icon name="Film" class="w-4.5 h-4.5" />
                    <Icon name="Send" class="w-4.5 h-4.5" />
                    <Icon name="Heart" class="w-4.5 h-4.5" />
                    <Icon name="PlusSquare" class="w-4.5 h-4.5" />
                  </div>
                  
                  <!-- Instagram Main Feed Mockup -->
                  <div class="flex-grow p-3 flex flex-col gap-3 overflow-hidden bg-white dark:bg-black">
                    <!-- Feed Stories Row -->
                    <div class="flex gap-2.5 overflow-hidden py-1 border-b border-[#efefef] dark:border-[#262626] pb-3">
                      <div class="flex flex-col items-center gap-0.5 flex-shrink-0">
                        <div class="w-8.5 h-8.5 rounded-full p-[1.5px] bg-gradient-to-tr from-yellow-500 via-red-500 to-purple-600">
                          <div class="w-full h-full rounded-full bg-white dark:bg-black border border-white dark:border-black flex items-center justify-center font-bold text-[7px]">BS</div>
                        </div>
                        <span class="text-[7px] text-gray-500 dark:text-gray-400 truncate w-9 text-center">betasquad</span>
                      </div>
                      <div class="flex flex-col items-center gap-0.5 flex-shrink-0">
                        <div class="w-8.5 h-8.5 rounded-full p-[1.5px] bg-gradient-to-tr from-yellow-500 via-red-500 to-purple-600">
                          <div class="w-full h-full rounded-full bg-white dark:bg-black border border-white dark:border-black flex items-center justify-center font-bold text-[7px]">DP</div>
                        </div>
                        <span class="text-[7px] text-gray-500 dark:text-gray-400 truncate w-9 text-center">dishapat...</span>
                      </div>
                      <div class="flex flex-col items-center gap-0.5 flex-shrink-0">
                        <div class="w-8.5 h-8.5 rounded-full p-[1.5px] bg-gradient-to-tr from-yellow-500 via-red-500 to-purple-600">
                          <div class="w-full h-full rounded-full bg-white dark:bg-black border border-white dark:border-black flex items-center justify-center font-bold text-[7px]">MB</div>
                        </div>
                        <span class="text-[7px] text-gray-500 dark:text-gray-400 truncate w-9 text-center">mercedes...</span>
                      </div>
                      <div class="flex flex-col items-center gap-0.5 flex-shrink-0">
                        <div class="w-8.5 h-8.5 rounded-full p-[1.5px] bg-gradient-to-tr from-yellow-500 via-red-500 to-purple-600">
                          <div class="w-full h-full rounded-full bg-white dark:bg-black border border-white dark:border-black flex items-center justify-center font-bold text-[7px]">UM</div>
                        </div>
                        <span class="text-[7px] text-gray-500 dark:text-gray-400 truncate w-9 text-center">__umwami</span>
                      </div>
                    </div>
                    
                    <!-- Post Block -->
                    <div class="flex flex-col gap-1.5 text-left">
                      <div class="flex items-center justify-between">
                        <div class="flex items-center gap-1.5">
                          <div class="w-5 h-5 rounded-full bg-yellow-600 dark:bg-yellow-700 flex items-center justify-center text-[7px] font-bold text-white">RM</div>
                          <span class="text-[8px] font-bold">realmadrid</span>
                          <span class="text-blue-500 text-[8px]">&bull;</span>
                          <span class="text-gray-400 dark:text-gray-500 text-[7px]">6 h</span>
                        </div>
                        <Icon name="MoreHorizontal" class="w-3 h-3 text-gray-400 dark:text-gray-500" />
                      </div>
                      <!-- Bellingham Image -->
                      <div class="relative w-full aspect-[4/3] rounded-md overflow-hidden bg-gray-900 border border-[#efefef] dark:border-[#262626]">
                        <img src="/bellingham_celebration.png" alt="Celebration Post" class="w-full h-full object-cover" />
                        <div class="absolute top-1.5 right-1.5 bg-black/60 p-1 rounded-full text-white cursor-pointer hover:bg-black/80 transition-colors">
                          <Icon name="Download" class="w-3 h-3 text-white" />
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
                
                <!-- Right StoryPeek Sidebar (38% width, adapts on mobile to fill 100%) -->
                <div class="w-full sm:w-[38%] flex flex-col bg-[#f8f9fa] dark:bg-[#0b0f14] text-[#262626] dark:text-[#f5f5f5] text-[10px]">
                  <!-- Header -->
                  <div class="px-2.5 py-2 border-b border-[#e2d9ce] dark:border-[#262626] flex items-center justify-between bg-white dark:bg-[#0d0f14]">
                    <div class="flex items-center gap-1">
                      <span class="w-4.5 h-4.5 rounded bg-[#6366f1] flex items-center justify-center text-white p-0.5">
                        <svg width="10" height="10" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
                          <circle cx="12" cy="12" r="4"/>
                          <path d="M12 2v2m0 16v2M4.22 4.22l1.42 1.42m12.72 12.72 1.42 1.42M2 12h2m16 0h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/>
                        </svg>
                      </span>
                      <span class="font-heading font-extrabold text-[11px] leading-none">Story<span class="text-[#6366f1]">Peek</span></span>
                    </div>
                    <!-- Header Actions -->
                    <div class="flex items-center gap-1.5 text-gray-400">
                      <Icon name="RefreshCw" class="w-3 h-3 hover:text-black dark:hover:text-white cursor-pointer" />
                      <Icon name="Instagram" class="w-3 h-3 hover:text-black dark:hover:text-white cursor-pointer" />
                      <Icon name="ExternalLink" class="w-3 h-3 hover:text-black dark:hover:text-white cursor-pointer" />
                      <Icon name="Moon" class="w-3 h-3 hover:text-black dark:hover:text-white cursor-pointer" />
                      <Icon name="Settings" class="w-3 h-3 hover:text-black dark:hover:text-white cursor-pointer" />
                    </div>
                  </div>

                  <!-- Tabs -->
                  <div class="px-3 border-b border-[#e2d9ce] dark:border-[#262626] bg-white dark:bg-[#0d0f14] flex justify-between text-[9px] font-semibold text-gray-500 dark:text-gray-400">
                    <span class="py-1.5 border-b border-[#6366f1] text-[#6366f1] cursor-pointer">Stories</span>
                    <span class="py-1.5 hover:text-black dark:hover:text-white cursor-pointer">Download</span>
                    <span class="py-1.5 hover:text-black dark:hover:text-white cursor-pointer">Bulk</span>
                    <span class="py-1.5 hover:text-black dark:hover:text-white cursor-pointer">Pro</span>
                  </div>

                  <!-- Green Active Banner -->
                  <div class="m-2 p-1.5 bg-emerald-50 dark:bg-emerald-950/20 border border-emerald-200/40 dark:border-emerald-900/30 rounded text-emerald-800 dark:text-emerald-400 flex items-center gap-1 text-[8px] leading-tight font-medium">
                    <Icon name="ShieldCheck" class="w-3 h-3 text-emerald-600 dark:text-emerald-400 flex-shrink-0" />
                    <span>Anonymous mode active — no seen receipts</span>
                  </div>

                  <!-- Content Area scrollable -->
                  <div class="flex-grow flex flex-col gap-3.5 px-2.5 overflow-y-auto max-h-[220px]">
                    <!-- Active Stories -->
                    <div class="flex flex-col gap-1 text-left">
                      <div class="flex justify-between items-center text-[8px] font-bold text-gray-400 uppercase tracking-wider">
                        <span>Active Stories</span>
                        <span class="text-emerald-600 dark:text-emerald-400">64 Active</span>
                      </div>
                      <div class="flex gap-1.5 py-1 items-center justify-between">
                        <!-- Bubble 1 -->
                        <div class="flex flex-col items-center gap-0.5">
                          <div class="w-8.5 h-8.5 rounded-full p-[1.5px] bg-gradient-to-tr from-yellow-500 via-red-500 to-purple-600 relative">
                            <div class="w-full h-full rounded-full bg-white dark:bg-[#0d0f14] border border-white dark:border-black flex items-center justify-center font-bold text-[7px] text-[#262626] dark:text-[#f5f5f5]">BS</div>
                            <span class="absolute -bottom-1 -right-0.5 bg-[#6366f1] text-white font-mono text-[7px] font-bold w-3 h-3 rounded-full flex items-center justify-center border border-white dark:border-black">2</span>
                          </div>
                          <span class="text-[7px] text-gray-500 dark:text-gray-400 truncate w-9 text-center">betasquad</span>
                        </div>
                        <!-- Bubble 2 -->
                        <div class="flex flex-col items-center gap-0.5">
                          <div class="w-8.5 h-8.5 rounded-full p-[1.5px] bg-gradient-to-tr from-yellow-500 via-red-500 to-purple-600 relative">
                            <div class="w-full h-full rounded-full bg-white dark:bg-[#0d0f14] border border-white dark:border-black flex items-center justify-center font-bold text-[7px] text-[#262626] dark:text-[#f5f5f5]">MB</div>
                            <span class="absolute -bottom-1 -right-0.5 bg-[#6366f1] text-white font-mono text-[7px] font-bold w-3 h-3 rounded-full flex items-center justify-center border border-white dark:border-black">2</span>
                          </div>
                          <span class="text-[7px] text-gray-500 dark:text-gray-400 truncate w-9 text-center">mercedes...</span>
                        </div>
                        <!-- Bubble 3 -->
                        <div class="flex flex-col items-center gap-0.5">
                          <div class="w-8.5 h-8.5 rounded-full p-[1.5px] bg-gradient-to-tr from-yellow-500 via-red-500 to-purple-600 relative">
                            <div class="w-full h-full rounded-full bg-white dark:bg-[#0d0f14] border border-white dark:border-black flex items-center justify-center font-bold text-[7px] text-[#262626] dark:text-[#f5f5f5]">DP</div>
                            <span class="absolute -bottom-1 -right-0.5 bg-[#6366f1] text-white font-mono text-[7px] font-bold w-3 h-3 rounded-full flex items-center justify-center border border-white dark:border-black">5</span>
                          </div>
                          <span class="text-[7px] text-gray-500 dark:text-gray-400 truncate w-9 text-center">dishapat...</span>
                        </div>
                        <!-- Bubble 4 -->
                        <div class="flex flex-col items-center gap-0.5">
                          <div class="w-8.5 h-8.5 rounded-full p-[1.5px] bg-gradient-to-tr from-yellow-500 via-red-500 to-purple-600 relative">
                            <div class="w-full h-full rounded-full bg-white dark:bg-[#0d0f14] border border-white dark:border-black flex items-center justify-center font-bold text-[7px] text-[#262626] dark:text-[#f5f5f5]">UM</div>
                            <span class="absolute -bottom-1 -right-0.5 bg-[#6366f1] text-white font-mono text-[7px] font-bold w-3 h-3 rounded-full flex items-center justify-center border border-white dark:border-black">2</span>
                          </div>
                          <span class="text-[7px] text-gray-500 dark:text-gray-400 truncate w-9 text-center">__umwami</span>
                        </div>
                      </div>
                    </div>

                    <!-- Recent Highlights -->
                    <div class="flex flex-col gap-1 text-left">
                      <div class="flex justify-between items-center text-[8px] font-bold text-gray-400 uppercase tracking-wider">
                        <span>Recent Highlights</span>
                        <span class="text-[#6366f1] hover:underline cursor-pointer">See all</span>
                      </div>
                      <div class="flex gap-2 py-0.5 overflow-hidden">
                        <div class="flex flex-col items-center gap-0.5">
                          <div class="w-7 h-7 rounded-full border border-gray-300 dark:border-gray-700 bg-white dark:bg-[#0d0f14] flex items-center justify-center text-[6px] font-bold">BS</div>
                          <span class="text-[6px] text-gray-500 dark:text-gray-400 truncate w-7 text-center">Highligh...</span>
                        </div>
                        <div class="flex flex-col items-center gap-0.5">
                          <div class="w-7 h-7 rounded-full border border-gray-300 dark:border-gray-700 bg-white dark:bg-[#0d0f14] flex items-center justify-center text-[6px] font-bold">MB</div>
                          <span class="text-[6px] text-gray-500 dark:text-gray-400 truncate w-7 text-center">Highligh...</span>
                        </div>
                        <div class="flex flex-col items-center gap-0.5">
                          <div class="w-7 h-7 rounded-full border border-gray-300 dark:border-gray-700 bg-white dark:bg-[#0d0f14] flex items-center justify-center text-[6px] font-bold">DP</div>
                          <span class="text-[6px] text-gray-500 dark:text-gray-400 truncate w-7 text-center">Highligh...</span>
                        </div>
                      </div>
                    </div>

                    <!-- Recently Viewed -->
                    <div class="flex flex-col gap-1 text-left">
                      <div class="flex justify-between items-center text-[8px] font-bold text-gray-400 uppercase tracking-wider">
                        <span>Recently Viewed</span>
                        <span class="text-gray-400 hover:text-black dark:hover:text-white cursor-pointer">Clear</span>
                      </div>
                      <div class="flex gap-2.5 py-0.5 overflow-hidden">
                        <div class="flex flex-col items-center gap-0.5">
                          <div class="w-7.5 h-7.5 rounded-full bg-yellow-500 flex items-center justify-center font-bold text-white text-[7px]">RD</div>
                          <span class="text-[7px] text-gray-500 dark:text-gray-400 truncate w-9 text-center">ronaldinho</span>
                        </div>
                        <div class="flex flex-col items-center gap-0.5">
                          <div class="w-7.5 h-7.5 rounded-full bg-blue-700 flex items-center justify-center font-bold text-white text-[7px]">RM</div>
                          <span class="text-[7px] text-gray-500 dark:text-gray-400 truncate w-9 text-center">realmadrid</span>
                        </div>
                        <div class="flex flex-col items-center gap-0.5">
                          <div class="w-7.5 h-7.5 rounded-full bg-[#00a6ff] flex items-center justify-center font-bold text-white text-[7px]">FT</div>
                          <span class="text-[7px] text-gray-500 dark:text-gray-400 truncate w-9 text-center">feastables</span>
                        </div>
                      </div>
                    </div>
                  </div>

                  <!-- Footer Status Bar -->
                  <div class="mt-auto px-2.5 py-1.5 bg-[#f8f9fa] dark:bg-[#0d0f14] border-t border-[#e2d9ce] dark:border-[#262626] flex items-center justify-between text-[7px] text-gray-400 rounded-b-xl">
                    <div class="flex items-center gap-1">
                      <span class="w-1 h-1 rounded-full bg-emerald-500"></span>
                      <span>64 stories found</span>
                    </div>
                    <div class="flex gap-1.5 font-medium">
                      <span>Help</span>
                      <span>Privacy</span>
                      <span>Terms</span>
                    </div>
                  </div>
                </div>
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="py-24 bg-surface border-b border-border">
    <div class="max-w-6xl mx-auto px-6">
      <div class="max-w-xl flex flex-col gap-4 mb-16 text-left">
        <h2 class="text-h2 text-text">Engineered for utility.</h2>
        <p class="text-body text-muted max-w-prose">
          Here is exactly what {ext.data.title} brings to your browsing workflow. No fluff, just functionality.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-12 text-left">
        {ext.data.features.map((feat) => (
          <div class="flex flex-col items-start gap-4">
            <div class="w-12 h-12 rounded-xl bg-accent/5 border border-accent/15 flex items-center justify-center text-accent">
              <Icon name={feat.icon} class="w-6 h-6 text-accent" />
            </div>
            <h3 class="text-h3 text-text">{feat.title}</h3>
            <p class="text-small text-muted max-w-prose">
              {feat.description}
            </p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Detail Markdown Content Section -->
  <section class="py-24 bg-bg">
    <div class="max-w-3xl mx-auto px-6 text-left">
      <h2 class="text-h2 text-text mb-8 border-b border-border/50 pb-4">Deep Dive & Technical Insights</h2>
      <div class="prose prose-stone leading-relaxed text-muted flex flex-col gap-6 text-base">
        <Content />
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="py-24 bg-surface border-t border-border">
    <div class="max-w-3xl mx-auto px-6">
      <div class="text-center flex flex-col gap-4 mb-16">
        <h2 class="text-h2 text-text">Frequently Asked Questions</h2>
        <p class="text-body text-muted max-w-prose mx-auto">
          Everything you need to know about permissions, data policies, and functionality.
        </p>
      </div>

      <div class="flex flex-col gap-4 text-left">
        {ext.data.faq.map((item) => (
          <details class="group bg-surface border border-border rounded-xl transition-all duration-200 [&_summary::-webkit-details-marker]:hidden">
            <summary class="flex justify-between items-center p-6 font-semibold text-text cursor-pointer focus:outline-none select-none">
              <span class="font-heading pr-4">{item.question}</span>
              <span class="transition-transform duration-200 group-open:rotate-180 text-muted">
                <Icon name="ChevronDown" class="w-5 h-5 text-muted" />
              </span>
            </summary>
            <div class="px-6 pb-6 text-sm text-muted leading-relaxed border-t border-border/30 pt-4">
              <p class="max-w-prose">{item.answer}</p>
            </div>
          </details>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>

<style is:global>
  /* Markdown subcontent rendering */
  .prose h2 {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-text);
    margin-top: 1.5rem;
    font-family: var(--font-heading);
  }
  .prose p {
    color: var(--color-muted);
  }
  .prose ul {
    list-style-type: disc;
    padding-left: 1.25rem;
  }
  .prose li {
    margin-bottom: 0.25rem;
  }
  .prose strong {
    color: var(--color-text);
  }
</style>
